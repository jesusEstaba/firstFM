import React, { Component } from 'react';
import {
  StyleSheet,
  Text,
  View
} from 'react-native';

export default class TopTracks extends Component {
    render() {
        return (
            <Text style={ {fontSize:30} }>Los top tracks</Text>
        )
    }
}