import React, { Component } from 'react';
import {
  StyleSheet,
  Text,
  View
} from 'react-native';

export default class ArtistTrack extends Component {
    render() {
        return (
            <Text style={ {fontSize:30} }>Esta es la cancion</Text>
        )
    }
}